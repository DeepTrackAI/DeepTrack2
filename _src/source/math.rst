math
====

.. automodule:: deeptrack.math

Module classes
<<<<<<<<<<<<<<

Average
^^^^^^^

.. autoclass:: deeptrack.math.Average
   :members:
   :exclude-members: get, add_child, add_dependency, add_feature, invalidate, is_valid, recurse_children, recurse_dependencies, seed, set_value, valid_index, validate, sample, action, resolve, previous, current_value, store

AverageBlur
^^^^^^^^^^^

.. autoclass:: deeptrack.math.AverageBlur
   :members:
   :exclude-members: get, add_child, add_dependency, add_feature, invalidate, is_valid, recurse_children, recurse_dependencies, seed, set_value, valid_index, validate, sample, action, resolve, previous, current_value, store

AveragePooling
^^^^^^^^^^^^^^

.. autoclass:: deeptrack.math.AveragePooling
   :members:
   :exclude-members: get, add_child, add_dependency, add_feature, invalidate, is_valid, recurse_children, recurse_dependencies, seed, set_value, valid_index, validate, sample, action, resolve, previous, current_value, store

BilateralBlur
^^^^^^^^^^^^^

.. autoclass:: deeptrack.math.BilateralBlur
   :members:
   :exclude-members: get, add_child, add_dependency, add_feature, invalidate, is_valid, recurse_children, recurse_dependencies, seed, set_value, valid_index, validate, sample, action, resolve, previous, current_value, store

Blur
^^^^

.. autoclass:: deeptrack.math.Blur
   :members:
   :exclude-members: get, add_child, add_dependency, add_feature, invalidate, is_valid, recurse_children, recurse_dependencies, seed, set_value, valid_index, validate, sample, action, resolve, previous, current_value, store

BlurCV2
^^^^^^^

.. autoclass:: deeptrack.math.BlurCV2
   :members:
   :exclude-members: get, add_child, add_dependency, add_feature, invalidate, is_valid, recurse_children, recurse_dependencies, seed, set_value, valid_index, validate, sample, action, resolve, previous, current_value, store

Clip
^^^^

.. autoclass:: deeptrack.math.Clip
   :members:
   :exclude-members: get, add_child, add_dependency, add_feature, invalidate, is_valid, recurse_children, recurse_dependencies, seed, set_value, valid_index, validate, sample, action, resolve, previous, current_value, store

GaussianBlur
^^^^^^^^^^^^

.. autoclass:: deeptrack.math.GaussianBlur
   :members:
   :exclude-members: get, add_child, add_dependency, add_feature, invalidate, is_valid, recurse_children, recurse_dependencies, seed, set_value, valid_index, validate, sample, action, resolve, previous, current_value, store

MedianBlur
^^^^^^^^^^

.. autoclass:: deeptrack.math.MedianBlur
   :members:
   :exclude-members: get, add_child, add_dependency, add_feature, invalidate, is_valid, recurse_children, recurse_dependencies, seed, set_value, valid_index, validate, sample, action, resolve, previous, current_value, store

NormalizeMinMax
^^^^^^^^^^^^^^^

.. autoclass:: deeptrack.math.NormalizeMinMax
   :members:
   :exclude-members: get, add_child, add_dependency, add_feature, invalidate, is_valid, recurse_children, recurse_dependencies, seed, set_value, valid_index, validate, sample, action, resolve, previous, current_value, store

NormalizeQuantile
^^^^^^^^^^^^^^^^^

.. autoclass:: deeptrack.math.NormalizeQuantile
   :members:
   :exclude-members: get, add_child, add_dependency, add_feature, invalidate, is_valid, recurse_children, recurse_dependencies, seed, set_value, valid_index, validate, sample, action, resolve, previous, current_value, store

NormalizeStandard
^^^^^^^^^^^^^^^^^

.. autoclass:: deeptrack.math.NormalizeStandard
   :members:
   :exclude-members: get, add_child, add_dependency, add_feature, invalidate, is_valid, recurse_children, recurse_dependencies, seed, set_value, valid_index, validate, sample, action, resolve, previous, current_value, store

Pool
^^^^

.. autoclass:: deeptrack.math.Pool
   :members:
   :exclude-members: get, add_child, add_dependency, add_feature, invalidate, is_valid, recurse_children, recurse_dependencies, seed, set_value, valid_index, validate, sample, action, resolve, previous, current_value, store

